# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# For local development, use your local PostgreSQL instance
# For Render, this will be automatically set from the database service
DATABASE_URL=postgresql://username:password@localhost:5432/bus_hazards

# Example for Render (will be auto-provided):
# DATABASE_URL=postgresql://user:password@hostname:5432/database_name
